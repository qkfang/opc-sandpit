# LEGO Site

This is a simple static LEGO-themed webpage.

## Open locally

- Directly open `lego-site/index.html` in a browser, or
- Serve it locally:

```bash
cd lego-site
python -m http.server 8000
```

Then visit `http://localhost:8000`.
